sap.ui.define(
    [
        "sap/fe/core/PageController"
    ],
    function (PageController) {
        "use strict";

        return PageController.extend("supplierintelhub.ext.view.SupplierObjectPage", {
            // Header + Spend Development chart are fully annotation/binding driven.
            // No custom logic required on the object page.
        });
    }
);
